<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Create A Figure</title>
  </head>
  <body>
    <form class="" action="/figures/<%=@figure.id%>/edit" method="post">

      <label>Name:</label><input type="text" name="figure[name]" value="<%=@figure.name%>" id="figure_name">
    </br></br>Title:</br>
      <%Title.all.each do |title|%>
      <%@figure.title_ids.include?(title.id) ? checked = "checked" : checked = "" %>
        <label><%=title.name%>:</label><input type="checkbox" name="figure[title_ids][]" value="<%=title.id%>" id="title_<%=title.id%>" "<%=checked%>"></br>
      <%end%>
    <label>New Title:</label> <input type="text" name="new_title" value="" id=title[name]></br>
    </br>Landmark:</br>
      <%Landmark.all.each do |landmark|%>
      <%@figure.landmark_ids.include?(landmark.id) ? checked = "checked" : checked = "" %>
        <label><%=landmark.name%>:</label><input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>" id="landmark_<%=landmark.id%>" "<%=checked%>"></br>
      <%end%>
      <label>New Landmark:</label> <input type="text" name="new_landmark" value="" id=landmark[name]></br>
      <input type="submit" name="Edit Figure" value="Edit Figure">
      <input name="_method" type="hidden" value="patch">
    </form>
  </body>
</html>
