<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Create A Figure</title>
  </head>
  <body>
    <form class="" action="/figures/new" method="post">
      <label>Name:</label><input type="text" name="figure[name]" value="" id="figure_name">
    </br></br>Title:</br>
      <%Title.all.each do |title|%>
        <label><%=title.name%>:</label><input type="checkbox" name="figure[title_ids][]" value="<%=title.id%>" id="title_<%=title.id%>"></br>
      <%end%>
    <label>New Title:</label> <input type="text" name="new_title" value="" id=title[name]></br>
    </br>Landmark:</br>
      <%Landmark.all.each do |landmark|%>
        <label><%=landmark.name%>:</label><input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>" id="landmark_<%=landmark.id%>"></br>
      <%end%>
      <label>New Landmark:</label> <input type="text" name="new_landmark" value="" id=landmark[name]></br>
      <input type="submit" name="Create New Figure" value="Create New Figure">
    </form>
  </body>
</html>
